{
  "name": "dynamodb-kinesis-fanout",
  "description": "",
  "version": "0.1.0",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "start": "sls offline start",
    "deploy_dev": "sls deploy --stage dev --region eu-central-1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.119",
    "serverless": "^3.33.0",
    "serverless-iam-roles-per-function": "^3.2.0",
    "serverless-plugin-typescript": "^2.1.5",
    "typescript": "^5.1.6"
  }
}
